DATABASE_URL="postgresql://postgres.jyefpueeemljccqdrehv:Addahmemoh%401@aws-0-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require"
GOOGLE_API_KEY=AQ.Ab8RN6KoJMmxgEUeS-7tJV008dOOvA4OuMm7Qn5MA-5aJJ5GyA


# Project Kestrel - local environment
# Save this file as ".env" in the project root. It is gitignored; never commit it.

# ---------------------------------------------------------------------------
# Carry these two over from your existing .env, but paste the ROTATED values.
# Both of the originals were transmitted during this session and should be
# treated as compromised: reset the database password in Supabase
# (Project settings > Database > Reset password) and delete and regenerate the
# Gemini key in Google AI Studio.
#
# Keep the ?pgbouncer=true / port 6543 pooler form your original URL used -
# main.py and database.py pass statement_cache_size=0 specifically for it.
# ---------------------------------------------------------------------------
DATABASE_URL="postgresql://postgres.jyefpueeemljccqdrehv:Addahmemoh%401@aws-0-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require"
GOOGLE_API_KEY=AQ.Ab8RN6KoJMmxgEUeS-7tJV008dOOvA4OuMm7Qn5MA-5aJJ5GyA

# ---------------------------------------------------------------------------
# New in fix 2. Signs the session tokens issued by auth.py.
# Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
# Changing it invalidates every live session, which is the intended behaviour.
# ---------------------------------------------------------------------------
JWT_SECRET="w7iWWMboNCERrjSRU4VCW87guGZga15KyZe8jEUaWA_cqcW6SnIdM2N3CxubfF5k"
SESSION_TTL_MINUTES=30

# ---------------------------------------------------------------------------
# Where agent.py looks for the API.
# ---------------------------------------------------------------------------
FASTAPI_BASE_URL="http://127.0.0.1:8000"

# ---------------------------------------------------------------------------
# CORS origins for the Flutter client, comma separated. These must be exact
# origins - Starlette's allow_origins does not expand wildcards like
# "http://localhost:*", so list each port you actually serve from.
# ---------------------------------------------------------------------------
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:8080"